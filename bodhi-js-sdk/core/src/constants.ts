/**
 * Core constants for Bodhi SDK
 */

/**
 * Default API request timeout in milliseconds
 * Used by all clients (direct and extension modes) for API requests
 * Default: 30 seconds
 */
export const DEFAULT_API_TIMEOUT_MS = 30000;
