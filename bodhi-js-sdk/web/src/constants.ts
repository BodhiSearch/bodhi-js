/**
 * Polling constants for extension detection
 */
export const POLL_INTERVAL = 500;
export const POLL_TIMEOUT = 5000;

/**
 * Default API request timeout in milliseconds
 * Used for API requests through window.bodhiext
 * Default: 30 seconds
 */
export const DEFAULT_API_TIMEOUT_MS = 30000;
